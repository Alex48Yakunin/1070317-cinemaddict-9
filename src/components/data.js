import {getFilmCard} from './getFilmCard';

const dataFilm = [
  getFilmCard(),
  getFilmCard(),
  getFilmCard(),
  getFilmCard(),
  getFilmCard(),
  getFilmCard(),
  getFilmCard(),
  getFilmCard(),
  getFilmCard(),
  getFilmCard(),
];

export {dataFilm};
